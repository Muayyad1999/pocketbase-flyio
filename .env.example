# ====================================
# PocketBase Environment Configuration
# ====================================

# Copy this file to .env and fill in your values
# cp .env.example .env

# ====================================
# General Configuration
# ====================================

# Enable verbose debug mode (set to "true" for development)
# POCKETBASE_DEBUG=false

# PocketBase server port (default: 8090)
# POCKETBASE_PORT_NUMBER=8090

# Additional PocketBase CLI options
# Example: --indexFallback --queryTimeout=30
# POCKETBASE_OPTS=

# ====================================
# Admin User Configuration
# ====================================

# Admin user email (will be created on first start if set)
# POCKETBASE_ADMIN_EMAIL=admin@example.com

# Admin user password
# For production, use a strong password or Docker secrets
# POCKETBASE_ADMIN_PASSWORD=your-secure-password-here

# Always update admin credentials from env vars on startup (default: true)
# Set to "false" to only create admin on first startup
# POCKETBASE_ADMIN_UPSERT=true

# ====================================
# Security & Encryption
# ====================================

# Encryption key for application settings (HIGHLY RECOMMENDED for production)
# Protects OAuth2 client secrets, SMTP passwords, etc.
# Generate with: openssl rand -base64 32
# POCKETBASE_ENCRYPTION_KEY=

# Alternative: Use Docker secrets file (recommended for production)
# POCKETBASE_ENCRYPTION_KEY_FILE=/run/secrets/encryption_key

# ====================================
# Directory Configuration
# ====================================

# Base working directory (default: /pocketbase)
# POCKETBASE_WORKDIR=/pocketbase

# Data directory for SQLite database (default: ${POCKETBASE_WORKDIR}/data)
# POCKETBASE_DATA_DIR=/pocketbase/data

# Migrations directory (default: ${POCKETBASE_WORKDIR}/migrations)
# POCKETBASE_MIGRATION_DIR=/pocketbase/migrations

# Static files directory (default: ${POCKETBASE_WORKDIR}/public)
# POCKETBASE_PUBLIC_DIR=/pocketbase/public

# JavaScript hooks directory (default: ${POCKETBASE_WORKDIR}/hooks)
# POCKETBASE_HOOK_DIR=/pocketbase/hooks

# ====================================
# Fly.io Deployment Examples
# ====================================

# For Fly.io, use secrets instead of .env file:
# fly secrets set POCKETBASE_ENCRYPTION_KEY="$(openssl rand -base64 32)"
# fly secrets set POCKETBASE_ADMIN_EMAIL="admin@example.com"
# fly secrets set POCKETBASE_ADMIN_PASSWORD="your-secure-password"
# fly secrets set POCKETBASE_DEBUG="false"

# ====================================
# Docker Compose Example
# ====================================

# For docker-compose.yml, add under the 'environment' section:
# environment:
#   - POCKETBASE_DEBUG=false
#   - POCKETBASE_ADMIN_EMAIL=admin@example.com
#   - POCKETBASE_ADMIN_PASSWORD=secure-password
#   - POCKETBASE_ENCRYPTION_KEY=your-32-char-key-here

# ====================================
# Production Recommendations
# ====================================

# 1. Always set POCKETBASE_ENCRYPTION_KEY in production
# 2. Use strong passwords (min 20 characters, alphanumeric + symbols)
# 3. Set POCKETBASE_DEBUG=false in production
# 4. Use Docker secrets or cloud provider secret management
# 5. Regularly backup your database
# 6. Keep PocketBase updated (auto-updates are configured!)

# ====================================
# References
# ====================================

# PocketBase Docs: https://pocketbase.io/docs/
# Going to Production: https://pocketbase.io/docs/going-to-production/
# Environment Variables: See README.md for complete list
