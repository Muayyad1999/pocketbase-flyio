# Database migrations

Deployment copy of the Flutter workspace's canonical `pocketbase/pb_migrations`. Update with `python scripts/sync_backend.py` from the Flutter workspace. Existing migrations must remain immutable.
